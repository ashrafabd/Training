<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href='/public/css/bootstrap.min.css' type="text/css">
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <title>
        <%= pageTitle %>
    </title>
</head>

<body id="page-top" class="min-vh-100 d-flex flex-column">

    <%- include('partials/navbar.ejs') %>

    <div class="d-flex justify-content-center" >
        <img src="<%= img_url %>" alt="<%= pokeName %>" width="30%">
        <div class="p-3">
          <h2 class="text-primary"><%= pokeName %></h2>
          <p class="h3"> This pokemon we see in the img called <span class="text-primary"> <%= pokeName %> </span></p>
          <p class="h3">The <span class="text-primary"> <%= pokeName %></span> pokemon has <%= poke_abilities.length %> abilities </p>
          <ul>
            <% poke_abilities.forEach((item)=>{ %>
                <li class="text-success h4"> <%= item.ability.name %> </li>
                This ability is
                <% if(item.is_hidden ===false){ %>
                    <span class="text-primary">Available</span>
                <% } else { %>
                    <span class="text-primary">Not Available</span>
                <% } %>

            <% }) %>
          </ul>

          <p class="h5">This pokemon has a  <span class="text-success"><%= poke_moves.length %> </span> moves</p>
        </div>
      </div>

    <%- include('partials/footer.ejs') %>


    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
        crossorigin="anonymous"></script>
</body>

</html>